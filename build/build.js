var WIDTH = 800;
var HEIGHT = 600;
function setup() {
    WIDTH = windowWidth;
    createCanvas(WIDTH, HEIGHT);
}
function draw() {
    background(0);
}
function windowResized() {
    createCanvas(WIDTH, HEIGHT);
}
//# sourceMappingURL=build.js.map